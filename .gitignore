# Go build
*.exe
*.test
*.out
bin/
dist/
coverage.out
coverage-full.out
/kacho-loadbalancer
/migrator

# Newman snapshots and run logs (audit trail, локально, не commit)
tests/newman/out/snap*-local-*.json
tests/newman/out/snap*-local-run.log
tests/newman/out/baseline-yc-*.json

# k6 load-test summary output (generated by tests/k6/*.js handleSummary).
tests/k6/*.summary.json

# Vendored corelib-owned infra protos — single source is kacho-corelib/proto.
# Pulled in transiently by `make proto-vendor` before buf lint/generate for import
# resolution only; NOT generated here and NOT committed (Go-stubs live in kacho-corelib).
/proto/google/
/proto/kacho/cloud/api/
/proto/kacho/cloud/operation/
/proto/kacho/cloud/validation.proto
/proto/kacho/iam/

# IDE
.idea/
.vscode/
.DS_Store
.claude/worktrees/
go.work
go.work.sum

# AI-оснастка (источник истины — kacho-workspace, локально синкается) — не коммитим
.claude/
CLAUDE.md
AGENTS.md
.codex/
.agents/
